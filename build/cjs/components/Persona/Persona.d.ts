import React from 'react';
import type { PersonaProps } from './types';
export declare function Persona({ size, theme, hasBorder, type, onClick, onClose, text, image, className, style, }: PersonaProps): React.JSX.Element;
export declare namespace Persona {
    var displayName: string;
}
